{
  "name": "simple-dummy",
  "version": "1.3.0",
  "description": "simple fastify dummy rest service",
  "main": "dist/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "build": "esbuild src/server.js --bundle --platform=node --format=cjs --minify --outfile=dist/server.js",
    "start": "node src/server.js"
  },
  "author": "Alexander Zenger",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/a-zen/simple-server"
  },
  "dependencies": {
    "fastify": "^5.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
